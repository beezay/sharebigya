/*
 * TableCalculator Messages
 *
 * This contains all the text for the TableCalculator component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TableCalculator';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TableCalculator component!',
  },
});

/*
 * SidebarJs Messages
 *
 * This contains all the text for the SidebarJs component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SidebarJs';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SidebarJs component!',
  },
});

/*
 * ScreenerTable Messages
 *
 * This contains all the text for the ScreenerTable component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ScreenerTable';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ScreenerTable component!',
  },
});

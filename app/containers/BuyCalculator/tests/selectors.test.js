// import { selectBuyCalculatorDomain } from '../selectors';

describe('selectBuyCalculatorDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

/*
 * BuyCalculator Messages
 *
 * This contains all the text for the BuyCalculator container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BuyCalculator';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BuyCalculator container!',
  },
});

/*
 * HeatMap Messages
 *
 * This contains all the text for the HeatMap container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.HeatMap';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HeatMap container!',
  },
});

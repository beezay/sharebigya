// import { selectPeriodicReturnWeeklyDomain } from '../selectors';

describe('selectPeriodicReturnWeeklyDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

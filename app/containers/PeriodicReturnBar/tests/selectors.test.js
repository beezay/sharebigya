// import { selectPeriodicReturnBarDomain } from '../selectors';

describe('selectPeriodicReturnBarDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

/*
 * TopBroker Messages
 *
 * This contains all the text for the TopBroker container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TopBroker';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TopBroker container!',
  },
});

/*
 * BarChart Messages
 *
 * This contains all the text for the BarChart container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BarChart';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BarChart container!',
  },
});

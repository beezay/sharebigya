/*
 * AdvDec Messages
 *
 * This contains all the text for the AdvDec container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AdvDec';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AdvDec container!',
  },
});

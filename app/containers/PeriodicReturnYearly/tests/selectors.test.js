// import { selectPeriodicReturnYearlyDomain } from '../selectors';

describe('selectPeriodicReturnYearlyDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

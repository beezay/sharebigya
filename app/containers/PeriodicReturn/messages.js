/*
 * PeriodicReturn Messages
 *
 * This contains all the text for the PeriodicReturn container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PeriodicReturn';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PeriodicReturn container!',
  },
});

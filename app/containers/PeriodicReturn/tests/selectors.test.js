// import { selectPeriodicReturnDomain } from '../selectors';

describe('selectPeriodicReturnDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

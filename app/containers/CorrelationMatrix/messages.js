/*
 * CorrelationMatrix Messages
 *
 * This contains all the text for the CorrelationMatrix container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CorrelationMatrix';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CorrelationMatrix container!',
  },
});

/*
 * TreeMap Messages
 *
 * This contains all the text for the TreeMap container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TreeMap';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TreeMap container!',
  },
});

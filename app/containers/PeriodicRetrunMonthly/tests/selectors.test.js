// import { selectPeriodicRetrunMonthlyDomain } from '../selectors';

describe('selectPeriodicRetrunMonthlyDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
